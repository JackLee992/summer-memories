# Blender 动画 → Seedance 视频参考精制实验

2026-09-21。用户明确要求将当前三维建模动画输出给 Seedance 做 video2video，观察更精致的 3D 动画版本。实际使用 Dreamina CLI 暴露的 `multimodal2video`（全能参考）入口，上传一条本地视频，不是纯文本生成。

## 已完成任务

- 模型：`seedance2.0_vip`。
- 请求：4 秒、1:1、1080p；共提交一次。
- `submit_id`：`e936aa33-adc4-4f96-a377-e0f3f466c613`。
- 服务在提交时返回 `credit_count: 264`。后续查询也会重复该字段，不能累计当作多次收费；此处记录不是完整账单。
- 服务已返回 `success`，原始结果已下载并校验，详见 [generation.json](generation.json)。没有因排队而重复提交，归档后无需再查询。

## 实际输入

[上传的视频](input_reference_4s.mp4) 来自 `../shoulder-polish-2026-09-21/revised.mp4`，即上一轮最新的单手肩扛、接柄、双手击打、回肩松手动画。

原视频 720×720、20 fps、72 帧、3.6 秒。仅在结尾补 0.4 秒末帧静止，变成 80 帧、4 秒，以匹配本轮最短输出时长。没有改变原动作速度，没有使用更早的中性人偶视频。

没有上传官方动画片段、演员素材、用户个人媒体或第三方模型。骨架及几何的来源和限制延续上一轮制作记录。

## 提示要求

[完整中文提示词](prompt.txt) 要求参考视频作为动作、镜头、构图和道具路径依据；精制成年女性的脸部、黑发、眼镜、西装织物、锤头金属、木柄与接触光照，同时保留单手肩扛 → 接柄 → 蓄力 → 一次下砸 → 卸力 → 回肩松手。保持固定三分之四全身镜头。

“全能参考”是以视频为条件重新生成画面，不保证逐帧复制。成片是视频像素，**不会返回升级后的 Blender 网格、材质、绑定或动作曲线**。

## 输出与观察

已保存 [原始 MP4](seedance_original.mp4)、[输入/输出对照页](comparison.html)、[相同时间取帧对照](frame_comparison.jpg) 和 [GIF 预览](seedance_preview.gif)。

实际结果为 1440×1440、H.264 视频与 AAC 音轨，视频 97 帧、约 4.017 秒，容器约 4.064 秒。名义帧率 `r_frame_rate=60/1`，实际平均 `5820/241`（约 24.15 fps），不能称为完整 60 fps 动画。原文件完整解码通过，SHA-256 为 `0972d30167bd6f16113b8a5f9e1a27a89056fdcdb4c47d637b2b6c2d49a29554`。

静帧和近景对照可见：脸部、眼镜、手指、西装领口、锤头金属和木箱结构更细致；但锤头明显放大、锤柄加粗，镜头构图更近，脚和木箱底部被裁切，举锤时顶部也超出画面。长发变成较厚的片状体块。输入约 1.65 秒接触，输出在约 1.9 秒附近才完成下砸，这是抽帧观察，未作精确接触时刻测量。

因此本轮证明了视频参考能提高部分画面细节，**没有证明动作、比例和完整构图可稳定保持，也没有达到商业动画验收标准**。已检查取帧与原文件解码，未完成实时播放中的手部连续性检查或音轨耳听验收。对照页按钮未实测，本地文件页面受浏览器策略限制。

完整服务响应、签名下载地址保存在被忽略的 `Builds/seedance-v2v-2026-09-21/`，公开制作记录只保留必要参数、任务状态、文件哈希与结果。不恢复 Unity 玩法开发、不修改原 `.blend`、不覆盖之前版本。

## 查询脚本留档

```sh
python3 docs/motion/seedance-v2v-2026-09-21/query_task.py
```

此脚本只查询已提交任务，成功时保存唯一返回的 MP4，不会再次提交或再次生成。当前任务已完成，脚本仅作来源记录，暂停期间不运行。
